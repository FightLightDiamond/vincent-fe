import React, {memo, useMemo, useRef, useState, useReducer} from "react";
import Paragraph from "./Paragraph";

// Context
// CompA => CompB => CompC

const Context_ = () => {
  return (
    <>
      <Paragraph/>
    </>
  )
}

export default memo(Context_)
